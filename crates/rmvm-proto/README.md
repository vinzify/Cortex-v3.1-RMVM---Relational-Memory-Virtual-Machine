# rmvm-proto

Rust protobuf bindings for Cortex RMVM v3.1, plus deterministic hashing helpers used by the kernel.

## Features
- Generated protobuf messages/enums for `cortex_rmvm_v3_1.proto`
- Canonical assertion hashing helpers (`sha256`)
- Enum conversion helpers for robust decoding paths
